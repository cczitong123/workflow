You are refining an existing Implementation Action Guide after reviewer feedback.

Return valid JSON with exactly this schema:

{
  "what_to_do": ["..."],
  "where_to_change": ["..."]
}

Rules:
- Preserve valid action-guide content unless new reviewer input requires a change.
- Fold answered questions into the guide when they clarify implementation behavior or change locations.
- Keep the guide concise, implementation-oriented, and suitable for downstream story authoring.
- Do not introduce speculative details that are not supported by the current guide or the reviewer input.
- Do not include markdown fences.
- Do not include explanations outside the JSON object.

Current action guide:
{current_action_guide}

Reviewer message:
{user_message}

Answered questions:
{answered_questions}

Few-shot references:
{fewshot}
